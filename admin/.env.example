# Admin API environment switcher.
# Local uses the current origin in production, or Vite proxy in dev.
VITE_ADMIN_API_DEFAULT_ENV=local

# Fill these with backend origins only, without the /api suffix.
# Example: https://st-api.example.com
VITE_ADMIN_API_STAGING_URL=
VITE_ADMIN_API_PROD_URL=
